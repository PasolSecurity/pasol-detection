# Stage 2 roadmap

1. G — feature catalog and PE golden fixtures (current foundation)
2. H — signed rule-pack format and full operator coverage
3. I — YARA-X isolated worker with resource limits
4. J — offline local reputation database and mock provider
5. K — managed metadata, scripts, LNK, archives, Office, PDF, MSI/CAB, and ISO parsers in `pasol-parser`
6. L — reproducible authorized-corpus logistic baseline and model card

Production signing keys, restricted datasets, and release environments must be supplied outside the public repository.
