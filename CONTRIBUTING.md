# Contributing

Use harmless deterministic fixtures. Run `cargo fmt --all`, workspace tests, and Clippy with warnings denied before review. Changes must preserve schema compatibility and include evidence-focused tests. Do not add unsafe Rust or active-content execution.
